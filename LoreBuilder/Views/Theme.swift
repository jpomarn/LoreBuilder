//
//  Theme.swift
//  LoreBuilder
// Creates the background,card and accent colors 

import SwiftUI

struct Theme {
    
    static let background = Color(red: 0.97, green: 0.92, blue: 0.92)
    
    static let card = Color(red: 0.95, green: 0.85, blue: 0.85)
    
    static let accent = Color(red: 0.86, green: 0.60, blue: 0.70)
}
